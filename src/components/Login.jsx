import React from 'react';
import useTitle from '../hooks/useTitle';

const Login = () => {
    useTitle("Login Pageeee");
    return (
        <div>
            Login Page
        </div>
    );
};

export default Login;